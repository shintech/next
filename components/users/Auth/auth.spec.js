/* eslint-env jest */

// import { shallow } from 'enzyme'
// import Auth from './index'

console.log = jest.fn()

describe('COMPONENT -> USERS -> Auth', () => {
  // let auth = shallow(<Auth />)

  it(`expect h1 text to equal...`, () => {
    expect()
    // expect(auth.find('h1').text()).toEqual('login')
  })
})
